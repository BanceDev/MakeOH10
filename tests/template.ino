#define RED 2
#define YELLOW 3
#define GREEN 4
#define BLUE 5
#define BUZZER 6


void setup() {
    pinMode(RED, OUTPUT);
    pinMode(YELLOW, OUTPUT);
    pinMode(GREEN, OUTPUT);
    pinMode(BLUE, OUTPUT);
    pinMode(BUZZER,OUTPUT);
}